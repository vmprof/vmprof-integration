sudo: required
dist: trusty
language: python
python:
  - "3.5"

addons:
  apt:
    packages:
      - python-virtualenv

install:
  - wget https://dl.google.com/linux/direct/google-chrome-stable_current_amd64.deb
  - sudo dpkg -i google-chrome*.deb
  - pip install -r requirements.txt
  - pip install --pre vmprof

before_script:
  - export CHROME_BIN=/usr/bin/google-chrome
  - export DISPLAY=:99.0
  - sh -e /etc/init.d/xvfb start
  - sleep 3 # give xvfb some time to start
  # download the chrome driver
  - wget http://chromedriver.storage.googleapis.com/2.22/chromedriver_linux64.zip
  - unzip chromedriver_linux64.zip
  - sudo mv chromedriver /usr/bin
  - git clone git://github.com/vmprof/vmprof-server.git
  - python vmprof-server/manage.py migrate
  - python vmprof-server/vmlog/test/data/loggen.py
  - python vmprof-server/manage.py loaddata vmlog/test/fixtures.yaml
  - python vmprof-server/manage.py runserver -v 3 &
  - sleep 3 # wait for django

script:
  - py.test testvmprof -v

notifications:
  irc:
    channels: "irc.freenode.org#baroque-dev"
    template:
      - "%{repository}@%{branch}: %{message} (%{build_url})"
    use_notice: true
    on_success: always
    on_failure: always

  email:
    on_success: change
    on_failure: change
